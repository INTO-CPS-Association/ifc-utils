"""Read-only exploration of IFC building models.

Four modules:
    explorer.py   opens a model and answers questions about it
    __main__.py   reads the command line and prints
    to_scene.py   writes the geometry as JSON, for reading
    to_glb.py     writes the geometry as GLB, for the viewer

See README.md in this folder.
"""
