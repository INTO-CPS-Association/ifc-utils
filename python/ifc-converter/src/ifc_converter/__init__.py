"""Turn an IFC model into files a viewer can read.

Reading and reporting on a model is `ifc_explorer`, which this depends on. This
package only produces artifacts: geometry, a property tree, and the binding
manifest that ties a sensor to an object in the model.
"""
